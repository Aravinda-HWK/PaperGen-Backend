describe('PaperController', () => {
  it('should be defined', () => {});

  it('should create a new paper', async () => {});

  it('should throw an error if the paper already exists', async () => {});
});
